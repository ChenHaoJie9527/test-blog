---
import "../styles/globals.css"
import Header from "./components/Header.astro";
import Footer from "./components/Footer.astro";
const pageTitle = "关于我"
const identity = {
    firstName: "沙拉",
    country: "中国",
    occupation: "前端工程师",
    hobbies: ["编程", "阅读", "旅行"],
}
const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Vue", "Node.js", "Git"]
const happy = true;
const finnish = false;
const goal = 3;
const skillColor = "blue";
const fontWeight = "bold";
const textCase = "uppercase";
---
<html lang="en">
  <head>
    <meta charset ="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{pageTitle}</title>
  </head>
  <style define:vars={{skillColor, fontWeight, textCase}}>
    h1 {
        color: purple;
        font-size: 4em;
    }
    .skill {
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
    }
  </style>
  <body>
    <Header />
    <h1>{pageTitle}</h1>
    <h2>……和我的新 Astro 网站！</h2>

    <p>我正在学习 Astro 的入门教程。这是我网站上的第二个页面，也是我自己建立的第一页面！</p>

    <p>随着我完成更多教程，该站点将更新，所以请继续查看我的旅程将如何进行吧！</p>
    <p>以下是我的个人信息：</p>
    <ul>
        <li class="skill">姓名：{identity.firstName}</li>
        <li class="skill">国籍：{identity.country}</li>
        <li class="skill">职业：{identity.occupation}</li>
        <li class="skill">爱好：{identity.hobbies.join(", ")}</li>
    </ul>
    <p>以下是我的技能：</p>
    <ul>
        {skills.map((skill) => (
            <li>{skill}</li>
        ))}
    </ul>

    {happy && <p>我非常乐意学习 Astro！</p>}

    {finnish && <p>我完成了这节教程！</p>}

    {goal === 3 ? <p>我的目标是在三天内完成。</p> : <p>我的目标不是 3 天。</p>}
    <Footer />
  </body>
</html>